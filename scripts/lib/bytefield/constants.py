"""
布局常量定义模块。

此模块定义了字节字段图表的布局常量，
便于后续调整和维护。
"""

# 图表总宽度（像素）
DIAGRAM_TOTAL_WIDTH = 768

# 默认左边距（像素）
DEFAULT_LEFT_MARGIN = 16

# 默认右边距（像素）
DEFAULT_RIGHT_MARGIN = 16

# 默认每行盒子数量
DEFAULT_BOXES_PER_ROW = 32

# 默认行高（像素）
DEFAULT_ROW_HEIGHT = 30

# 默认字号（像素）
DEFAULT_FONT_SIZE = 12
